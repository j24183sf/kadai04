## 自動販売機（データ構造と各種処理）のミニレポート
### 課題4-3-1. 自動販売機の商品データついて説明せよ。
* データ構造（各項目とその説明）
* {id:商品番号、name:商品名、price:値段、stock:在庫数}をひとまとまりとしてデータをitemsに入れている。
* items[n]と書くとitemsのn番目を持ってくるが、items[n].idと書くとitemsのn番目のidにくっついている商品番号を持ってきてくれる。
* 連想配列の配列として定義するメリット
* 関連するデータをひとまとまりにするためデータ管理がしやすい。
* キーを使ってデータを呼び出すと、何のデータを持ってきているのかわかりやすい。
### 課題4-3-2. showItemListの処理内容について説明せよ。
* 配列の繰り返し処理
* for文でi=0~7を繰り返す。
* items[0]のidは1なのでiをいじる必要はない。
* あとはconsole.log("文字:" + items[i].キー +...)で商品情報を出す。
* 連想配列の参照方法
* items[i].idで商品番号、items[i].nameで商品名、items[i].priceで値段、items[i].stockで在庫数を参照している。
### 課題4-3-3. buyItemの処理内容について説明せよ。
* 商品購入の可否判定
* if(0 < no && no <= items.length)でid以内の数か判定する。if(items[no-1].stock > 0)で在庫があるか判定する。
* 商品在庫を減らす処理
* if(items[no-1].stock > 0){items[no-1].stock -= 1;}でstockの数が0以上だった場合、在庫を減らす。
* 商品番号のエラー処理
* if(0 < no && no <= items.length)でfalseだった場合、エラーメッセージ「nは無効な商品番号です。番号は1～8で指定してください。」が出る。if(items[no-1].stock > 0)falseだった場合、「在庫がありません。」が出る。
### 課題4-3-4. プログラムの考察
* データ構造について
* 連想配列でデータがひとまとまりになっているのと、[キー:データ]なので見やすい。
* 引数として使う場合もitems[i].キーなので、itemsのキーの情報なんだねってわかる。
* 商品一覧表示と購入処理を関数化したメリット
* 商品一覧表示と購入処理を一々書くより見やすい。
* 関数化すると、一行で呼び出せるので効率的。
### 課題4-3-5. 感想
* 今回の課題で苦労したこと
* VScodeでgitコマンドが使えない
* 演習を通して理解できたこと
* console.log(line);で改行
* この自動販売機プログラムの追加機能や課題など
* ホットかアイスかをitemsに追加する。
* 投入金額の表示、購入商品との計算、お釣りの表示。
